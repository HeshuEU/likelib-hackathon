
image: ubuntu:19.10
stages:
  - create
  - ls
Creating files:
  tags:
    - docker
  stage: create
  script: 
     - echo "Start create container"
     - mkdir build
     - touch ./build/1
     - touch ./build/2
     - echo "Docker container create successfully"
  artifacts:
    paths:
      - build

View files:
  tags:
    - docker
  stage: ls
  script:
    - ls -l
    - ls ./build -l
